

@using PantryParty.Models;
@using Microsoft.AspNet.Identity;


@*<table>
    <tr>
        <th>Name</th>
    </tr>
    @foreach (Recipe recipe in ViewBag.UsersRecList)
    {
        <tr>
            <td>
                <h4>@recipe.Title</h4>
            </td>
            <td>
                <img src="@recipe.ImageURL" width="75"height="75" />
            </td>
            <td>
                @*<form action="/Home/DeleteRecipe" method="get">
                    <input type="hidden" value="@User.Identity.GetUserId()" name="CurrentUser" />
                    <input type="hidden" value="@recipe." name="ItemToDelete" />
                    <input type="submit" value="Delete" class="btn button" />
                </form>*@
            @*</td>
        </tr>
    }
</table>*@